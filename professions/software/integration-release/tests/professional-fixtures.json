{
  "schema_version": "0.1",
  "profession_id": "integration-release-engineer",
  "fixtures": [
    {
      "id": "stale-branch",
      "expected": "preserve bounded delta plus newer accepted state; no blind merge"
    },
    {
      "id": "semantic-overlap",
      "expected": "detect cross-file contract conflict and HOLD/consult"
    },
    {
      "id": "wrong-composition-green",
      "expected": "reject transferred green evidence; require exact-head proof"
    },
    {
      "id": "rollback-gap",
      "expected": "HOLD release until recoverable prior state exists"
    },
    {
      "id": "code-readiness-executable-contract",
      "expected": "Report bounded readiness without granting release or acceptance authority. Executed by tests/validate-code-workflow.mjs and tests/code-workflow-integration.mjs; full body remains EXPERIMENTAL."
    }
  ]
}
